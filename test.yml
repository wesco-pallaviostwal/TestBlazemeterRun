execution:
  - scenario: sample_test
    executor: blazemeter
    concurrency: 1
    ramp-up: 1m
    hold-for: 2m

scenarios:
  sample_test:
    requests:
      - url: https://www.google.com
        method: GET
